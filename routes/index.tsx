import { PageProps } from "$fresh/server.ts";

export default function Home(_props: PageProps) {
  return (
    <main>
      <h1>Invio Site</h1>
      <p>Fresh + Deno basic starter. No styling yet.</p>
    </main>
  );
}
